{
  "name": "dust-tt/dust",
  "build": {
    "dockerfile": "../dev/Dockerfile",
    "context": ".."
  },
  "install": "bash dev/scripts/install.sh",
  "start": "i=0; until [ -f dev/scripts/wait-for-workspace.sh ]; do i=$((i+1)); [ \"$i\" -gt 240 ] && echo \"timed out waiting for workspace checkout\" >&2 && exit 1; sleep 0.5; done; bash dev/scripts/wait-for-workspace.sh --then infra.sh",
  "terminals": [
    {
      "name": "mprocs",
      "command": "i=0; until [ -f dev/scripts/wait-for-workspace.sh ]; do i=$((i+1)); [ \"$i\" -gt 240 ] && echo \"timed out waiting for workspace checkout\" >&2 && exit 1; sleep 0.5; done; bash dev/scripts/wait-for-workspace.sh --then apps.sh",
      "description": "Dev stack via mprocs: sdks-js/sparkle watches, bacon Rust reload, all services"
    }
  ],
  "ports": [
    {
      "name": "front-api",
      "port": 3000
    },
    {
      "name": "front-spa",
      "port": 3011
    },
    {
      "name": "core-api",
      "port": 3001
    },
    {
      "name": "viz",
      "port": 3007
    },
    {
      "name": "temporal",
      "port": 7233
    }
  ]
}
