// See note in constants.ts — this is a marketing-side stub.
export const DEFAULT_REMOTE_MCP_SERVERS: ReadonlyArray<{
  name: string;
  description: string;
  icon: string;
  documentationUrl: string | null;
  authMethod: unknown | null;
  toolStakes?: Record<string, string>;
}> = [];
