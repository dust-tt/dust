const tracer = require('dd-trace').init({
  debug: true,
  service: process.env.DD_SERVICE || 'test-service',
  env: process.env.DD_ENV || 'test',
  version: process.env.DD_VERSION || '1.0.0',
  profiling: false,
  logInjection: true,
  runtimeMetrics: false,
  startupLogs: true
});
