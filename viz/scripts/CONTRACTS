@cc [owner:flvndvd,label:product] frame-types-front-copy
The Frame runtime types builder MUST copy the archive before atomically publishing its manifest
to the local Frame skill. If the Front skill directory is absent, the Viz build MUST skip this copy.
