{
  "buildCommand": "npm run build-storybook",
  "outputDirectory": "storybook-static",
  "ignoreCommand": "if [ -n \"$VERCEL_GIT_PREVIOUS_SHA\" ]; then git diff --quiet $VERCEL_GIT_PREVIOUS_SHA $VERCEL_GIT_COMMIT_SHA -- sparkle/src && exit 0 || exit 1; else exit 1; fi"
}
