import { SkillBuilderView } from "../components/SkillBuilderView";

// The skill builder page with the knowledge composer in its instructions
// section — the "/" menu seen in real page chrome (sticky bars, a scrolling
// container, the surrounding sections) rather than on its own.
export default function SkillBuilderKnowledge() {
  return <SkillBuilderView />;
}
