import { DustHomeView } from "../components/DustHomeView";

// The Dust homepage with the "/" composer standing in for the input bar, so
// the menu can be exercised where it actually has to work: a compact, bottom
// -anchored field in a 48rem column.
export default function DustHome() {
  return <DustHomeView />;
}
