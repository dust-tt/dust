import * as AspectRatioPrimitive from "@radix-ui/react-aspect-ratio";

/**
 * Constrains its children to a fixed width-to-height `ratio` (e.g. `16 / 9`), so
 * the box resizes responsively while keeping proportions. Use it to reserve
 * consistent space for media (images, video, embeds) and prevent layout shift.
 * @summary Fixed-ratio responsive container.
 */
const AspectRatio = AspectRatioPrimitive.Root;

export { AspectRatio };
