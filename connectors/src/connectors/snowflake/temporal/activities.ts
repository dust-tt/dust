export async function syncSnowflakeConnection(connectorId: number) {
  // TODO(SNOWFLAKE)
  // do SHOW TABLES on the Snowflake connection
  // look at DBs / Schemas / Tables with explicit read permissions
  // garbage collect / add missing resources as needed
  void connectorId;
}
