name: Scheduled Lint (front)

on:
  push:
    paths:
      - ".github/workflows/scheduled-tests-front.yml"
  schedule:
    # Run twice a day at 8 AM and 8 PM UTC
    - cron: "0 8,20 * * *"
  workflow_dispatch: # Allow manual triggering

permissions:
  contents: read
  actions: read
  checks: write

jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Notify Test Start
        id: start_notification
        uses: slackapi/slack-github-action@v2.0.0
        with:
          method: chat.postMessage
          token: ${{ secrets.SLACK_BOT_TOKEN }}
          payload: |
            channel: ${{ secrets.SLACK_FRONT_TESTS_NOTIFICATIONS_CHANNEL_ID }}
            text: |
              🧪 Starting scheduled front tests
              • Workflow: <${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}|View run>

      - name: Setup Node Dependencies
        uses: ./.github/actions/setup-node-deps
        with:
          build-sdks: "true"
          build-sparkle: "true"

      - name: Notify Test Success
        if: success()
        uses: slackapi/slack-github-action@v2.0.0
        with:
          method: chat.postMessage
          token: ${{ secrets.SLACK_BOT_TOKEN }}
          payload: |
            channel: ${{ secrets.SLACK_FRONT_TESTS_NOTIFICATIONS_CHANNEL_ID }}
            thread_ts: "${{ steps.start_notification.outputs.ts }}"
            text: |
              ✅ Scheduled front lint passed
              • Workflow: <${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}|View run>

      - name: Notify Test Failure
        if: failure()
        uses: slackapi/slack-github-action@v2.0.0
        with:
          method: chat.postMessage
          token: ${{ secrets.SLACK_BOT_TOKEN }}
          payload: |
            channel: ${{ secrets.SLACK_FRONT_TESTS_NOTIFICATIONS_CHANNEL_ID }}
            thread_ts: "${{ steps.start_notification.outputs.ts }}"
            text: |
              ❌ Scheduled front lint failed <!subteam^${{ secrets.SLACK_ENGINEERING_TEAM_GROUP_ID }}>
              • Workflow: <${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}|View run>
