# This GitHub Actions workflow was auto-generated by the `rdme` cli on 2024-06-25T13:33:11.644Z
# You can view our full documentation here: https://docs.readme.com/docs/rdme
name: Deploy OpenAPI Docs

on:
  workflow_dispatch:

jobs:
  rdme-openapi:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repo 📚
        uses: actions/checkout@v3

      - name: Run `openapi` command 🚀
        uses: readmeio/rdme@v8
        with:
          rdme: openapi public/swagger.json --key=${{ secrets.README_API_KEY }} --id=667ac6ef498a5b002ad0829b
