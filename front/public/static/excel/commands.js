/* global Office */

(function () {
    "use strict";

    // The initialize function must be run each time a new page is loaded
    Office.initialize = function (reason) {
        // If needed, initialize add-in when it's loaded
    };

    // Associate function names with UI events (if we add custom commands later)
    // For now, the ShowTaskpane action is handled automatically
})();