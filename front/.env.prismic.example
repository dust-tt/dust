# Prismic CMS Configuration
# Copy this file to .env.local and fill in the values

# Optional: Override the default repository name (defaults to "dust-blog" from slicemachine.config.json)
# NEXT_PUBLIC_PRISMIC_ENVIRONMENT=dust-blog

# Optional: Add access token for private repositories
# PRISMIC_ACCESS_TOKEN=your_access_token_here