export async function sendFirstReminderEmail({
  workspaceId,
}: {
  workspaceId: string;
}) {
  void workspaceId;
}

export async function sendSecondReminderEmail({
  workspaceId,
}: {
  workspaceId: string;
}) {
  void workspaceId;
}
