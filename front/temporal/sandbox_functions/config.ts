const QUEUE_VERSION = 2;
const LEGACY_QUEUE_VERSION = 1;

export const QUEUE_NAME = `sandbox-functions-queue-v${QUEUE_VERSION}`;
export const LEGACY_QUEUE_NAME = `sandbox-functions-queue-v${LEGACY_QUEUE_VERSION}`;
