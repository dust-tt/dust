export const GLOBAL_SKILL_SEARCH_ALIASES: Record<string, string[]> = {
  "go-deep": ["Deep Dive"],
  frames: ["Frames"],
  sandbox: ["Sandbox"], // Currently called "Computer", but makes sense to think of it as a sandbox.
};
